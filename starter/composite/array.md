---
Array (数组): 是值类型。长度固定，不可变。传参时会拷贝整个数组（性能差）。
Slice (切片): 是引用类型。长度动态。传参时只拷贝“切片头”（性能极高）。99% 的场景都用 Slice。
记忆: 想象 Slice 是一个窗口，透过它看底层的数组。
Slice 的结构体包含三个字段: [
  "Pointer：指向底层数组的起始位置。",
  "Length (len)：窗口里目前有多少个元素。",
  "Capacity (cap)：底层数组从起始位置到底，一共还能装多少个元素。"
]
---
